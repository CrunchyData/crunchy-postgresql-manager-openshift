{
    "kind": "Service",
    "apiVersion": "v1",
    "metadata": {
        "name": "{{.SERVICENAME}}",
        "labels": {
            "name": "{{.SERVICENAME}}"
        }
    },
    "spec": {
        "ports": [{
            "protocol": "TCP",
            "port": {{.PORT}},
            "targetPort": {{.PORT}},
            "nodePort": 0
        }],
        "selector": {
            "name": "{{.NAME}}"
        },
        "portalIP": "None",
        "type": "ClusterIP",
        "sessionAffinity": "None"
    },
    "status": {
        "loadBalancer": {}
    }
}
